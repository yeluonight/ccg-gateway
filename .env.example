# CCG Gateway 环境变量配置
# 在项目根目录创建 .env 文件来自定义配置
# 未创建 .env 文件则使用默认值

# ===== 服务器配置 =====
# API 服务器监听地址（默认: 0.0.0.0）
CCG_SERVER_HOST=0.0.0.0

# API 服务器端口（默认: 7788）
CCG_SERVER_PORT=7788

# ===== 数据库配置 =====
# 数据目录（默认: 系统应用数据目录）
# Windows: %APPDATA%\ccg-gateway
# macOS: ~/Library/Application Support/ccg-gateway
# Linux: ~/.config/ccg-gateway
CCG_DATA_DIR=/path/to/data

# 配置数据库路径（默认: <数据目录>/ccg_gateway.db）
CCG_DB_PATH=/path/to/ccg_gateway.db

# 日志数据库路径（默认: <数据目录>/ccg_logs.db）
CCG_LOG_DB_PATH=/path/to/ccg_logs.db

# ===== 日志配置 =====
# 是否启用文件日志（默认: false）
CCG_LOG_TO_FILE=true

# 日志级别：trace, debug, info, warn, error（默认: info）
CCG_LOG_LEVEL=info
